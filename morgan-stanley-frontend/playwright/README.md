# How to run
```
npx playwright test /tests --project=chromium
```

# Requirements
## add playwright.env at root of the playwright directory

# playwright.env needed variables
```
USERNAME=   #Username used for cloudfront
PASSWORD=   #Password used for cloudfront
HOMEPAGE=   #URL of the homepage, use http://XXXXXXXX.cloudfront.net/trades, else http://localhost:5137/trades
```
